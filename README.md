# Form Processor for MailChimp
Processor for WordPress forms sent to the MailChimp API

Remember to use `composer install --no-dev --prefer-dist;` to update the composer libraries before deploying a new tag.
